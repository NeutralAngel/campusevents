<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Campus Events" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <%= javascript_include_tag "application" %>
  </head>

  <body>
    <div id="root">
      <div id="root_footer">

        <%= render 'shared/topbar' %>

        <div class="row">
          <div id="main_window" class="large-8 columns">
            <%= yield %>
          </div>
          <div id="sidebar" class="large-4 columns">
            <%= yield :sidebar %>
          </div>
        </div>

      </div>
    </div>

    <%= render 'shared/footer' %>

    
  </body>


</html>